- provide :title, @model.name

#products.block.clear
  %h1= @model.name
  %table#results
    - rows = @harmonicas.each_slice(4).to_a
    - rows.each do |row|
      %tr
        - row.each do |harmonica|
          = render harmonica

  .clear
