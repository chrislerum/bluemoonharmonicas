=====
Order #<%= @order.id %>
=====

A packing slip is attached, but here's the order at-a-glance:

<% @order.cart.line_items.each do |line_item| %>
  -----
  <%= "#{line_item.comb.name} #{number_to_currency line_item.unit_price}" %>
    <% if line_item.special_instructions.present? %>
      <%= "Special instructions: #{line_item.special_instructions}" %>
    <% end %>
    <%= "Quantity: #{line_item.quantity}" %>
  -----
<% end %>

Total: <%= number_to_currency @order.cart.total_price %>

=====
Address
=====

<%= @order.address['Name'] %>
<%= @order.address['Street1'] %>
<% unless @order.address['Street2'].blank? %>
  <%= @order.address['Street2'] %>
<% end %>
<%= "#{@order.address['CityName']}, #{@order.address['StateOrProvince']} #{@order.address['PostalCode']}" %>
