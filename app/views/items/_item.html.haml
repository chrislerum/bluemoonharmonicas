%td
  = link_to item do
    - if item.main_image
      = image_tag item.main_image.photo, width: 250
    - else
      = image_tag 'default_main_image.jpg', width: 250
    %h3
      %span.controls
        = inventory_or_price_string_for(item)
      = link_to item.name, item
